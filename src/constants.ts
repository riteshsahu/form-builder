export enum QuestionType {
  TEXT = "text",
  NUMBER = "number",
  SELECT = "select",
}

export enum NumberType {
  DECIMAL = "decimal",
  INTEGER = "integer",
}

export const DEFAULT_FORM_TITLE = "Untitled Form";
